import renameProp from './rename-prop'
import changeComposition from './change-composition'
import sortImports from './sort-imports'

export const rules = {
  'rename-prop': renameProp,
  'change-composition': changeComposition,
  'sort-imports': sortImports,
}
